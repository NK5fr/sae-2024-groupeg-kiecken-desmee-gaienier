//import CryptoJS from 'crypto-js';

/*export default function encryptionTool(password) {
	const salt = 'HeavenMustFall';
	const combinedString = password + salt;
	const hash = CryptoJS.MD5(combinedString);
	return hash;
}*/
